import type { ReactElement } from "react";

export default function Loading(): ReactElement {
	/* DUMB change: we don't need this
	return (
		<div className="loading" data-testid="loading">
			&#8230;
		</div>
	);
	*/
	return <></>;
}
